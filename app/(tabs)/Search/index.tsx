import { defaultStyles } from '@/styles'
import { Text, View } from 'react-native'

const SearchScreen = () => {
	return (
		<View style={defaultStyles.container}>
			<Text style={defaultStyles.text}>Search Screen</Text>
		</View>
	)
}

export default SearchScreen
